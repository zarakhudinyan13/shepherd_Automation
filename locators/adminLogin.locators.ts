export const AdminLoginLocators = {
  usernameInput: '#username',
  passwordInput: '#password',
  loginButton: '#doLogin',
  roomsHeader: 'h2'  // We'll improve this after we inspect the header
} as const;
